certifications = chứng chỉ

one new one = phiên bản mới

estimates = ước tính

a standard of the code = đoạn mã chuẩn
effort = sự cố gắng
subsequent maintenance = bảo trì
echancement = nâng cấp
Agreeing on = đồng ý về, Việc thống nhất,...
we recommend it highly = khuyên bạn nên sử dụng nó
limitations - hạn chế
cramped = chật chỗ, khó đọc

curly braces??????

Consider yourself forewarned = coi như bạn được cảnh báo trước

sick and twisted = disgusting and terrible = kinh tởm và kinh khủng

complex concepts = khái niệm phức tạp

naming standard = tiêu chuẩn đặt tên

Integrated Development Environment = môi trường phát triển tích hợp

components = thành phần
 
properties = thuộc tính

retrive = truy xuất

property's value = giá trị thuộc tính

prefix = tiền tố

valid = có hiệu lực = hợp lệ

appropriate = hợp lý

signatures = chữ ký

represents(đại diện)

take no arguments = không tranh luận

multicast = phát đa hướng

occurs = xảy ra

argument = tham số đầu vào = biến đầu vào

package-centric = tập trung vào gói

assumed = tập trung

JVM = Java Virtual Machine = Máy ảo Java

explicit = rõ ràng

reverse = đảo ngược

appended = nối thêm

collisions = collide = va chạm = xung đột

schemes = kế hoạch

guaranteed = đảm bảo

assuming = giả sử

instance = Thực thể

independent = độc lập

 positioning rules discussed here = quy tắc được định vị ở đây
 
 statement = tuyền bố
 
 declaration = khai báo
 
 apply to = áp dụng cho
 
nested = inner =  lồng nhau, bên trong
