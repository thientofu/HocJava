package baitap.Lab11Hoc;

public class ProcessAndThreads {
    //In concurrent programming, there are two basic units of execution: processes and threads. In the Java programming language, concurrent programming is mostly concerned with threads. However, processes are also important.
    //Trong lập trình, có 2 thứ cơ bản là processes và threads. Ở trong ngôn ngữ lập trình Java, tất cả chương trình thì liên quan nhiều nhất với threads. tuy nhiên thì processes cũng rất quan trọng

    //Processes
    //A process has a self-contained execution environment. A process generally has a complete, private set of basic run-time resources; in particular, each process has its own memory space.
    //1 process có môi trường thực thi độc lập. 1 process có một bộ tài nguyên riêng, mỗi tiến trình có không gian bộ nhớ riêng

    //Threads
    //Threads are sometimes called lightweight processes. Both processes and threads provide an execution environment, but creating a new thread requires fewer resources than creating a new process.
    //Threads được gọi là processes nhẹ.cả processes và threads đều cung cấp môi trường thực thi, nhưng tạo 1 thread cần ít tài nguyên hơn 1 process
}
